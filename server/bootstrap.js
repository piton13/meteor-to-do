//Meteor.startup(function () {
    // code to run on server at startup
    //if (Projects.find({}).count()) return;


    /*Items.insert({
      title: 'Hellow'
    });

    Items.insert({
      title: 'World'
    });*/
//});
